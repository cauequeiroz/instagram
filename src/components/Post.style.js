import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    newComment: {
        flexDirection: 'row',
        alignItems: 'center',
        borderBottomWidth: 1,
        borderBottomColor: '#ddd'
    },
    newComment_input: {
        flex: 1,
        height: 40
    },
    newComment_image: {
        width: 30,
        height: 30
    }
})